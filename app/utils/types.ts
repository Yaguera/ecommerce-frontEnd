export interface Product {
    id: string;
    name: string; // Nome unificado para 'name'
    price: number; // Preço para uso no carrinho e exibição
    quantidade: number;
    imageUrl: string;
  }
  